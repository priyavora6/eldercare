import 'package:flutter/material.dart';

class FamilyLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF5F7FA),
      appBar: AppBar(
        title: Text('Family Login'),
      ),
      body: Center(
        child: Text('Family Login Screen'),
      ),
    );
  }
}
